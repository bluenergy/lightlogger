package processors

import (
	"time"
)

func StandaloneMod(dir string, begin time.Time, end time.Time) {
	//startPos := fastLocateStartPos(reader, begin)
	//process(reader, startPos, end, func(buf []byte) bool {
	//	utils.Notice(string(buf))
	//	return true
	//})
}